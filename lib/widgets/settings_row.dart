
import 'package:flutter/material.dart';

class SettingsRow extends StatelessWidget {
  const SettingsRow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Sozlamalar ekranidagi qatorlar uchun shablon
    return Container();
  }
}
