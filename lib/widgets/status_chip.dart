
import 'package:flutter/material.dart';

class StatusChip extends StatelessWidget {
  const StatusChip({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Kichik status yorliqlari uchun shablon
    return Container();
  }
}
