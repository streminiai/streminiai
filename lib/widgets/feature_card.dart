
import 'package:flutter/material.dart';

class FeatureCard extends StatelessWidget {
  const FeatureCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Bosh ekrandagi katta kartochkalar uchun shablon
    return Container();
  }
}
