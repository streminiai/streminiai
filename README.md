# stremini-app
## 📁 Project Folder Structure
frontend/   # UI & chat layout
backend/    # API endpoints
api/        # placeholder or Cloudflare Worker scripts
models/     # shared data models
docs/       # architecture, API docs, branch rules


✅ All devs will follow this structure.:

```markdown
## 🌿 Branch Naming Rules
- feature/<feature-name> → new features (e.g., feature/chat-ui)
- bugfix/<issue-name> → bug fixes (e.g., bugfix/input-lag)
- hotfix/<issue-name> → urgent production fixes (e.g., hotfix/startup-crash)

